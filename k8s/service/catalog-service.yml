apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: book2go
spec:
  selector:
    app: catalog-service
  ports:
    - protocol: TCP
      port: 8091
      targetPort: 8091
  type: ClusterIP