apiVersion: v1
kind: Service
metadata:
  name: reader-service
  namespace: book2go
spec:
  selector:
    app: reader-service
  ports:
    - protocol: TCP
      port: 8098
      targetPort: 8098
  type: ClusterIP