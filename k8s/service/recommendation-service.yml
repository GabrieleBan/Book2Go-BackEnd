apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: book2go
spec:
  selector:
    app: recommendation-service
  ports:
    - protocol: TCP
      port: 8096
      targetPort: 8096
  type: ClusterIP