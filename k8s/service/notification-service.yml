apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: book2go
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8092
      targetPort: 8092
  type: ClusterIP