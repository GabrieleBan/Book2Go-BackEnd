apiVersion: v1
kind: Service
metadata:
  name: lend-service
  namespace: book2go
spec:
  selector:
    app: lend-service
  ports:
    - protocol: TCP
      port: 8099
      targetPort: 8099
  type: ClusterIP