apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: book2go
spec:
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: 8095
      targetPort: 8095
  type: ClusterIP